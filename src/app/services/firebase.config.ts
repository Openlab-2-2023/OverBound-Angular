// Set your Firebase Realtime Database base URL here (no trailing slash).
// Example: https://your-project-id-default-rtdb.firebaseio.com
export const FIREBASE_DB_URL = '';

// NOTE: Leave empty to keep localStorage-only behavior. If set, AuthService
// will attempt to persist users under `/users/<encoded-email>.json`.
